package zero.extensions;

/**
 * **Usage:** 
 * 
 * To use ALL of the zero.extensions, simply: `using zero.extensions.Tools`
 */

class Tools {}

@:dox(hide)
typedef TArrayExt = ArrayExt;
@:dox(hide)
typedef TEnumExt = EnumExt;
@:dox(hide)
typedef TFloatExt = FloatExt;
@:dox(hide)
typedef TStringExt = StringExt;